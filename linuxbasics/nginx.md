\# Nginx Local Web Server



\## What is nginx?

Nginx is a web server most popular web server , replacing Apache 



\## What is a web server?

A web server is used to fetch and process clien't and system requests to a computer

\## What is localhost?

Localhost sits on your current laptop and fetches the current data 



\## What is /var/www/html?



it is common path where the document is stored



\## Commands practiced



curl localhost (shows doctype! in the terminal )

ls /var/www/html(shows where the current files are)

sudo nano index.html(create your own webpage)

sudo apt update

sudo apt intall nginx -y

sudo systemctl status nginx (legacy systems have service )

sudo systemctl start nginx

sudo systemctl status nginx

\## What I tested



\## Why this matters for AWS EC2



