Today I resolved the conflict in Git , starting by creating a text line and then switching to other branch changing the original text line , then committing and saving the changes , done the same with main branch too under the same file , so it creates naming conflict which I then resolved by changing the text file manually to only what I needed and then added ( add .) and committed with pushing one final time and resolved the issue of conflicting 



\###Commands used :

git checkout -b 

git checkout main

git merge



